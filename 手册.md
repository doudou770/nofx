# NOFX 本地运行手册

本手册将指导您如何在本地环境（不使用 Docker）分别运行 NOFX 的后端和前端。

## 1. 环境准备

在开始之前，请确保您的系统已安装以下软件：

*   **Go**: 版本 1.21 或更高 ([下载地址](https://go.dev/dl/))
*   **Node.js**: 版本 18 或更高 ([下载地址](https://nodejs.org/))
*   **TA-Lib**: 技术指标计算库 (必须安装，否则后端无法编译)
    *   **Windows**: 建议使用 `vcpkg` 安装或下载预编译的 `.dll`。
    *   **macOS**: `brew install ta-lib`
    *   **Linux**: `sudo apt-get install libta-lib0-dev`

## 2. 后端运行 (Backend)

后端负责核心交易逻辑、API 服务和数据库管理。

### 步骤 2.1: 初始化配置

在项目根目录下，复制示例配置文件：

```bash
# Windows (PowerShell)
Copy-Item config.json.example config.json

# macOS / Linux
cp config.json.example config.json
```

**注意**: `config.json` 包含系统基础配置。AI 模型和交易所的 API Key 现在推荐通过 Web 界面配置，而不是直接修改此文件。

### 步骤 2.2: 安装依赖

```bash
go mod download
```

### 步骤 2.3: 启动后端

```bash
# 直接运行
go run main.go

# 或者编译后运行 (推荐)
go build -o nofx.exe
.\nofx.exe
```

**成功信号**:
当您看到以下日志时，说明后端已启动成功：
```
╔════════════════════════════════════════════════════════════╗
║    🤖 AI多模型交易系统 - 支持 DeepSeek & Qwen            ║
╚════════════════════════════════════════════════════════════╝
...
🌐 API服务器启动在 http://localhost:8080
```

## 3. 前端运行 (Frontend)

前端提供可视化的操作界面。请打开一个新的终端窗口进行操作。

### 步骤 3.1: 进入前端目录

```bash
cd web
```

### 步骤 3.2: 安装依赖

```bash
npm install
```

### 步骤 3.3: 启动开发服务器

```bash
npm run dev
```

**成功信号**:
终端会显示访问地址，通常是：
```
  VITE v6.0.7  ready in 350 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

## 4. 开始使用

1.  打开浏览器，访问前端地址（通常是 `http://localhost:5173` 或 `http://localhost:3000`，具体看 `npm run dev` 的输出）。
2.  **配置 AI**: 在 Web 界面点击 "AI模型配置"，输入您的 DeepSeek 或 Qwen API Key。
3.  **配置交易所**: 点击 "交易所配置"，输入 Binance 或 Hyperliquid 的 API Key。
4.  **创建交易员**: 点击 "创建交易员"，组合 AI 和交易所，设置初始资金。
5.  **启动**: 点击交易员卡片上的 "启动" 按钮。

## 5. 常见问题

*   **后端报错 "missing ta-lib"**: 请检查 TA-Lib 是否正确安装并配置了环境变量。
*   **前端无法连接后端**: 默认情况下，前端会尝试连接 `localhost:8080`。如果您的后端端口不同，请检查 `config.json` 中的 `api_server_port` 配置。
*   **端口冲突**: 如果 8080 端口被占用，请修改 `config.json` 中的 `api_server_port`，并重启后端。

---
祝您交易顺利！🚀
